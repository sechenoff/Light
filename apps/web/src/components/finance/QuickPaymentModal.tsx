/**
 * QuickPaymentModal — thin re-export of RecordPaymentModal.
 * Kept for backward compatibility; new code should import RecordPaymentModal directly.
 */
export { RecordPaymentModal as QuickPaymentModal } from "./RecordPaymentModal";
