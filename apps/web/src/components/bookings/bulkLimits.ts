/**
 * Потолок одной пачки групповых действий. Зеркалит `BULK_MAX_IDS` в
 * `apps/api/src/services/bookingBulk.ts` — сервер отбивает превышение сам
 * (400 BULK_TOO_MANY), а клиент предупреждает ДО нажатия, чтобы оператор не
 * упирался в отказ после выбора сотни строк.
 *
 * При изменении править обе константы.
 */
export const BULK_MAX_IDS = 100;
