API_KEY=your_api_key_here
API_PROXY_TARGET=http://localhost:4000

# DEV ONLY — show quick-login buttons on /login (matched by backend DEV_AUTH_BYPASS).
# Set to 'true' only in local dev. The auth bypass is also gated by NODE_ENV !== 'production'.
NEXT_PUBLIC_DEV_AUTH_BYPASS=false
