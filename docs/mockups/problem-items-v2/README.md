# Потеряшки v2 — исследование и два концепта

Дата: 2026-09-17, обновлено 2026-09-18. Статус: **выбрана инвентаризация** — финальный мокап
`final-inventory.html`, ждёт утверждения перед спекой. Код не трогали.

## Что есть сейчас (по коду и проду)

| | Факт | Где |
|---|---|---|
| Единственный вход | «✗ Проблема» в чек-листе приёмки киоска. Вручную завести нельзя — ни из реестра, ни из киоска | `ReturnChecklist` → `completeSession.problemUnits` |
| Причины | 4, все про площадку клиента: остался / потерян / уничтожен / украден | `ProblemReason` |
| Статусы | ожидается / на поиске / найдено / не найдено / списано | `ProblemStatus` |
| Действия | «Найдено» (в оборот) / «Не найдено» (закрыто без денег). Авто-«найдено» при повторной приёмке | `problemItemService.ts` |
| Реестр | пилюли по статусу, таблица/карточки, красная подсветка просроченных «ожидается», ссылка на бронь. Нет поиска, ответственного, возраста, хроники, источника, KPI | `ProblemItemsPage.tsx` |
| Наличие | COUNT-потеряшка вычитается из доступности **только через `bookingItem.equipmentId`** — у ручной записи брони нет, вычесть её было бы неоткуда | `availability.ts:getLostCountByEquipmentMap` |
| Деньги | `// FUTURE` в `resolveProblemItem`: «не найдено» должно рождать долг клиента, но суммы взять неоткуда — стоимости замены в каталоге нет | `problemItemService.ts` |
| Прод | **1 запись за всё время** (COUNT, «потерян», комментарий «Вернут»). 295 позиций, **294 без штучного учёта**, 3 юнита всего | `prod.db`, 2026-09-17 |
| Приёмка на практике | из 435 возвращённых броней **с пересчётом в киоске приняты 4**; 143 закрыты автоматической сверкой, остальные — сменой статуса. Пропаж на возврате никто не видит | `ScanSession`, `AuditEntry BOOKING_RETURNED`, 2026-09-18 |
| Статусы отстают | 3 подтверждённые брони сегодня по датам на съёмке, но не отмечены выданными | `Booking`, 2026-09-18 |
| Форма парка | 1 808 шт в 295 позициях: 114 в одном экземпляре, 121 — по 2–5, 58 — больше 5 | `Equipment.totalQuantity` |
| Прецедент | Поломка вне брони уже умеет заводиться из киоска (`RegisterBreakageScreen`, вкладка «Поломки») и из раздела (`AddRepairModal`) | мастерская v2 |

Вывод: раздел не используется не потому, что пропаж нет, а потому что попасть в него можно
только с приёмки. Парк — счётный, значит любой новый вход должен работать с количествами,
а не с юнитами.

## Как это делают rental-системы

| Система | Что берём |
|---|---|
| **Rentman** — Inventory count → недостача автоматически попадает в *Lost equipment* (модуль Maintenance); lost уменьшает сток; «mark as found» возвращает; кладовщик может пометить lost и на возврате | сверка как источник; «найдено» одной кнопкой |
| **Current RMS / OnRent** — Inventory Check: охват фильтрами (склад/группа/теги), счёт сканом / вручную / CSV; непосчитанное висит в «Exceptions», посчитанное — в «OK»; излишек помечается *Found*; «Submit» создаёт stock-транзакции и *quarantine* (lost/damaged не бронируются); serialized-активы на прошлых заказах авто-принимаются, если физически есть | возобновляемая сессия; излишек = нашлось; ничего не пишется до «Завершить» |
| **Cheqroom** — audits и spot-checks по расписанию, флаг lost/missing прямо в поле, «last seen» (последний custodian) | «след» — кому последнему выдавали |
| **Booqable** — stock counts для bulk-позиций, чтобы находить недостачу | счёт количеств, не сканов |

Общий знаменатель: (1) потерю можно завести не только на возврате; (2) есть пересчёт полки,
который сам показывает расхождения; (3) потеря уменьшает доступность до «найдено»;
(4) «найдено» — одно действие; (5) списание — отдельный, явный исход; (6) видно, где вещь
была в последний раз; (7) есть ответственный и возраст дела; (8) видно деньги под риском.

Источники: [Rentman — Count your Inventory](https://support.rentman.io/hc/en-us/articles/360015960120-Count-your-Inventory),
[Rentman — Handling Lost Equipment](https://support.rentman.io/hc/en-us/articles/360014392200-Handling-Lost-Equipment),
[Rentman — Lost or Defective Equipment in the Warehouse](https://support.rentman.io/hc/en-us/articles/17531415206418-How-to-Handle-Lost-or-Defective-Equipment-in-the-Warehouse),
[Current RMS — Inventory Check](https://help.current-rms.com/en/articles/3532962-count-your-stock-using-inventory-check),
[Current RMS — Check-in products (lost/damaged → quarantine)](https://help.current-rms.com/en/articles/385106-check-in-return-products),
[Cheqroom — Tracking](https://www.cheqroom.com/capabilities/tracking/),
[Booqable — Inventory management](https://booqable.com/inventory-management/).

## Концепты

> Финал: **`final-inventory.html`** — см. «Рекомендация» ниже.

### A · `concept-a-reestr.html` — «Реестр расследований»
Эволюция текущего экрана. Потеряшка — открытое дело: что пропало, где видели, кто ищет,
сколько висит, чем закончится.
- «**Завести потеряшку**» из реестра (модалка, 3 шага) и из киоска (рядом с «Поломкой»);
- причина «**Не нашли на складе**» + поле **источник** (приёмка / сверка / вручную);
- «**След**» — последние брони, где позиция выдавалась (уже есть в `BookingItem`);
  в модалке — «вероятно здесь» → `sourceBookingId`;
- **ответственный** + «**📣 объявить в поиск**» (карточка на «Моём дне» у всех + задача);
- порядок **по срочности** (горит / в работе / закрытые за 30 дней), поиск, KPI-полоса;
- закрытие в 4 исхода: найдено / не найдено / **списать** / **компенсация в смету** (SA).

### B · `concept-b-sverka.html` — «Сверка склада»
Пропажи находят пересчётом полки, а не по одной.
- под-раздел «**Сверка склада**»: сессия с охватом по категориям, прогрессом, «Отложить»;
- «**по учёту на полке**» = всего − выдано − в мастерской − открытые потеряшки
  (та же формула, что у календаря);
- расхождение → «В потеряшки» / «Нашлось» / «Пересчитать»; запись — только по «Завершить»;
- излишек закрывает открытую потеряшку как «найдено»;
- реестр получает колонку «источник» и историю сверок; отметка «сверено N числа» у позиции;
- ручной ввод из A остаётся вторым путём.

## Сравнение

| | A · Реестр | B · Сверка |
|---|---|---|
| Закрывает просьбу «увидел, что не хватает — добавил, объявил в поиск» | да, напрямую | да, через сессию сверки |
| Схема | `ProblemItem`: + `equipmentId`, `source`, `assignedTo`, `announcedAt`; enum + `NOT_ON_SHELF` | то же **плюс** `StockCount`, `StockCountLine`, `Equipment.lastCountedAt` |
| Новых экранов | модалка + экран киоска + расширение реестра | сессия сверки (десктоп + киоск) + история + расширение реестра |
| Объём | ~3–4 дня | ~7–9 дней (включает A-минимум) |
| Риск | низкий: всё по образцу мастерской v2 | средний: новая сущность, формула «по учёту» должна совпадать с `availability.ts` один в один |
| Когда окупается | сразу: сегодня раздел пуст, потому что в него нельзя попасть | когда пересчёт полки станет регулярным (раз в 2–4 недели по категории) |

## Рекомендация (2026-09-18) — инвентаризация главная

Первая версия README советовала начать с ручного ввода (A). Данные прода это опровергают:
раздел пуст не потому, что в него нельзя попасть, а потому что **на приёмке никто не считает**
(4 возврата из 435). Кнопка «Добавить» не поможет — о пропаже просто некому узнать.
Узнать можно только пересчётом полки. Поэтому главное — инвентаризация, мокап
**`final-inventory.html`** (вырос из B, из A взяты «след» и ручной ввод).

**Что входит в первый релиз**
1. **Весь склад по умолчанию**, участки по категориям между людьми, «Пауза», строка
   сохраняется сразу.
2. **«На полке должно быть»** = всего − выдано − в мастерской − открытые потеряшки −
   **брони, которые по датам на съёмке, но не отмечены выданными** (иначе их позиции выглядят
   пропавшими). Ожидание фиксируется в момент счёта строки.
3. **Ввод под парк**: «на месте / нет на полке» для 114 позиций в одном экземпляре,
   степпер + «Всё на месте · N» для остальных.
4. **Три решения по расхождению**: «пропало → потеряшки», «ошибка учёта» (поправить
   количество в каталоге; только руководитель, с причиной), «пересчитать». Первая
   инвентаризация — прежде всего сверка учёта.
5. **«Как пропало»** — окно между пересчётами: брони с позицией с прошлой инвентаризации
   (в первый раз — 60 дней), как их принимали (с пересчётом / вручную / автоматически),
   подсказка вероятной брони. Где точнее сказать нельзя — так и пишем.
6. **Ничего не пишется до «Завершить»**: потеряшки, поправки учёта, закрытие найденного,
   отметки «сверено» — одной операцией.
7. **Акт инвентаризации** — PDF A4 альбомный + XLSX.
8. В потеряшках — источник записи, новая причина «Не нашли на складе» и ручное
   «Завести потеряшку» (маленький второй путь на том же фундаменте).

**Не входит:** компенсация клиенту и «стоимость замены», объявление в поиск, подсказки
«пора пересчитать». Решим после первой инвентаризации — будет видно, что нужно.

**Этапы:** 1) схема + формула «на полке» (общая с `availability.ts`) + сервис сессии;
2) киоск — счёт; 3) экран итога, решения, «как пропало»; 4) завершение одной транзакцией
+ акт; 5) потеряшки — источник и ручной ввод.

**Как провести первую:** в день, когда ничего не выдано; вдвоём, поделив категории; быть
готовыми, что большая часть расхождений — ошибки учёта. После неё «как пропало» начинает
работать по-настоящему: окно сужается до броней между пересчётами, а ходовые позиции
(удлинители, мешки, стойки — по 13–19 выдач за 60 дней) стоит принимать в киоске с пересчётом.

## Открытые вопросы владельцу

1. **Компенсация**: нужно ли поле «стоимость замены» у позиции — или сумму всегда вводит
   руководитель вручную? Для первого релиза не нужно, понадобится для «не найдено → долг клиента».
2. **Кто решает «ошибка учёта»**: только руководитель (как в мокапе) — или кладовщику тоже можно
   с обязательной причиной?
3. **Непосчитанные позиции**: завершать инвентаризацию с ними можно (они остаются «не сверены»)
   — или требовать пройти всё?
