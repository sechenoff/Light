{
  "sprint": "dead-code-cleanup",
  "claude_product": "n/a (behaviour-preserving cleanup — technical review only)",
  "technical_review": "APPROVE",
  "provider": "Claude code-reviewer",
  "head": "post-3ff48d2 + item3 commit",
  "ts": "2026-05-18T15:45:00Z",
  "scope": "dead code / dupes removal from product+codebase audit (behaviour-preserving)",
  "fixes_applied": [
    "Item 1: removed recordScan + POST /sessions/:id/scan + scanBodySchema + dead tests. barcode-integration.test.ts rewired recordScan->seedScan (direct prisma.scanRecord.create) — live completeSession/reconciliation coverage preserved (verified non-masking).",
    "Item 2: removed deprecated deletePayment + DELETE /api/payments/:id. Its only test rewritten as describe(voidPayment) with identical assertions — net void coverage preserved. Frontend already uses POST /:id/void. Gaffer's separate deletePayment untouched.",
    "Item 3 (frozen->resolved): removed reachable «📷 Открыть в сканере» Link from /equipment/[id]/units per user directive 'No Barcodes in UX'. /admin/scanner page + BarcodeScanner components kept dormant for future.",
    "Item 4: added scoped .gitignore (apps/api/dev*.db, root /*.png) — git check-ignore verified does NOT match tracked prisma/dev.db or docs/**/*.png. Repo copy-artifacts (* N.tsx etc) absent on this branch.",
    "Item 5 (frozen, no change): computeAging/computeAgingPerClient/computeForecast/buildClientDebtExport operate on Invoice (not Booking) — different entity, not duplicates of isBookingOverdue/computeOutstanding. Conservatively left intact to avoid money-exact regression for ~zero dedup value."
  ],
  "tests": "915/915 pass (dead-code-only tests removed; live coverage preserved via canonical-path rewiring)",
  "ts_check": "clean (api + web, incl. Item 3 edit)",
  "par_rounds": [
    "Round 1 (HEAD 3ff48d2): technical APPROVE — 0 CRIT/HIGH/MED/LOW, all 6 risk checks pass (no live code deleted, no test masking, no dangling refs, .gitignore correctly anchored, 915/915).",
    "Item 3 follow-up commit: scanner Link removed, tsc web clean exit 0, Link import still used."
  ],
  "followups": [
    "Doc: CLAUDE.md 'Scanner component is shared' line is now stale (units page no longer imports BarcodeScanner) — doc-only.",
    "2 .claude/data/sessions/*.json already git-tracked — untrack-or-keep is a repo-history decision, deferred."
  ]
}
