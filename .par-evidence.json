{
  "sprint": "critical-money-safety",
  "claude_product": "ACCEPTED",
  "technical_review": "APPROVE",
  "provider": "split-focus (two Claude reviewers — no codex this session)",
  "head": "bc0baf21",
  "ts": "2026-05-18T15:05:00Z",
  "scope": "4 CRITICAL bugs from product+codebase audit",
  "fixes_applied": [
    "C1: legacy POST /:id/confirm bypass closed — rolesGuard([SUPER_ADMIN]) + non-bot DRAFT/PENDING_APPROVAL -> 409 USE_APPROVAL_FLOW. Bot one-step flow preserved via botAccess (whitelisted openclaw key, by design). Dead action=='confirm' branch in /status removed.",
    "C2: releaseBookingUnits(bookingId, tx) — units returned AVAILABLE on cancel + cancel-with-deposit, BookingItemUnit cleared, MAINTENANCE/RETIRED skipped, idempotent, BOOKING_UNITS_RELEASED audit, all in one $transaction.",
    "C3: isBookingOverdue()/computeOutstanding() single-source helpers — dashboardMetrics/computeDebts/computeFinanceDashboard now compute overdue live (no stale paymentStatus drift, no cron). Decimal-correct.",
    "C5: lost-unit compensation now serializes invoiceNeedsReissue in /complete API response (was dropped at API layer) + warehouse/scan persistent rose warning on operation screen + INVOICE_OUTDATED_BY_COMPENSATION audit.",
    "C1-obs (PAR LOW): bot-channel confirm writes BOOKING_CONFIRMED_VIA_BOT audit (after.via:'bot', attributed to first SUPER_ADMIN due to AuditEntry.userId FK constraint)."
  ],
  "tests": "928/928 pass (was 927 + 1 new bot-confirm-audit test)",
  "ts_check": "clean (api + web)",
  "par_rounds": [
    "Round 1 (HEAD 2281165): technical APPROVE (0 CRIT/HIGH/MED, 1 LOW pre-existing double-reservation edge — follow-up note). product ACCEPTED + 1 MEDIUM (C5 flag silent in UI).",
    "Fix-pass (HEAD bc0baf21): C5 UI surfacing + API serialization root-cause + bot-confirm audit.",
    "Re-review: product ACCEPTED (MEDIUM closed). technical APPROVE stands (additive, 928/928)."
  ],
  "followups": [
    "LOW (technical): cross-booking unit-status edge if a unit is double-reserved by two bookings — pre-existing upstream invariant, separate ticket.",
    "Doc drift: CLAUDE.md scanSession.ts ref is warehouseScan.ts (doc-only, no code impact)."
  ]
}
