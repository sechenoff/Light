{
  "sprint": "payment-terms",
  "claude_product": "ACCEPTED",
  "technical_review": "APPROVE",
  "provider": "split-focus",
  "ts": "2026-04-27T16:00:00Z",
  "notes": [
    "Booking.expectedPaymentDate теперь работает end-to-end: schema OrganizationSettings.defaultPaymentTermsDays (Int default 7), backend auto-default при createBookingDraft + confirmBooking, UI поле в BookingForm + Settings, backfill script.",
    "Standard PAR: technical REQUEST_CHANGES (TZ drift на edit + Zod без datetime validator), product NEEDS_FIXES (no dynamic placeholder, no recompute on endDate edit, null inconsistency POST/PATCH). 8 fixes applied.",
    "Final gates: tsc clean api+web; 886 API + 256 web tests all green."
  ]
}
